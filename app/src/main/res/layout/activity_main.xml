<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_behavior=".features.home.presentation.behaviors.OverScrollBehavior">

        <com.google.android.material.appbar.CollapsingToolbarLayout
            android:id="@+id/collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:minHeight="?attr/actionBarSize"
            android:background="@color/warning_red"
            app:contentScrim="@color/done_green"
            app:layout_scrollFlags="scroll|exitUntilCollapsed" />

    </com.google.android.material.appbar.AppBarLayout>

    <TextView
        android:id="@+id/finished_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/header_padding_left"
        android:paddingBottom="18dp"
        android:text="Выполнено -- 2"
        app:layout_anchor="@id/app_bar_layout"
        app:layout_anchorGravity="bottom|left"
        android:elevation="4dp"/>

    <ImageView
        android:id="@+id/hide_button"
        android:layout_width="@dimen/hide_button_size"
        android:layout_height="@dimen/hide_button_size"
        android:layout_marginRight="@dimen/header_padding_default"
        android:layout_marginBottom="@dimen/header_padding_default"
        android:src="@drawable/ic_eye"
        app:layout_anchor="@id/app_bar_layout"
        app:layout_anchorGravity="bottom|right"
        android:elevation="4dp"/>

    <TextView
        android:id="@+id/task_not_exist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="tasks:"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/availability_blue"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/tasks_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />
    </androidx.core.widget.NestedScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
